<h1>Your current duels</h1>
Hi. Welcome, <%= current_player.name %>.

<h3>Active Matches</h3>
<ul>
<% current_player.matches.active.each do |m| %>
	<li>
		<%= link_to m.name, match_url(m)  %> 
		<%=  '(Your move)' if your_turn %>
	</li>
<% end%>
</ul>	

<h3>Completed Matches</h3>
<ul>
<% current_player.matches.completed.each do |m| %>
	<li>
		<%= link_to m.name, match_url(m) %> 
		(<%=  m.result %> 
<% if m.winning_player %>
<%= ", Winner: #{m.winning_player == current_player ? 'You' : m.winning_player.name}" %> 
<% end %>
		)

	</li>
<% end%>
</ul>	

<%= link_to "New Match", new_match_url %>
