(in C:/ChessOnRails/ChessOnRails)
Note: this document should be regenerated periodically by running
	rake test:doc

authentication_controller should:
 - have index login logout actions
 - only logged in can change security question
 - login security question only
 - test user dean can login
 - test user maria can login
 - reject incorrect login
 - reject move without login

match_controller should:

move_controller should:

board should:
 - knows a valid location and distinguishes between invalid one

game should:
 - 32 pieces on chess initial board
 - noone to move defaults to player1
 - next to move alternates sides

match should:
 - returns active matches

move should:
 - unstarted match has no moves
 - moves are returned in order created
 - white can make first move
 - black cannot make first move
 - can play first two moves correctly
 - cant move if its not your turn

piece should:
 - recognizes valid piece types
 - rejects invalid piece types
 - has a notation for king and queen
 - has a notation for minor and rook
 - has a notation for pawn

player should:
 - can be created
 - stores name
 - be savable
 - reject names that exceed allowed length
 - reject registering duplicate player names

user should:

